---
export interface Props {
  as?: 'div' | 'section' | 'article' | 'header' | 'footer' | 'main' | 'nav';
  class?: string;
}

const { as: Tag = 'div', class: className = '' } = Astro.props;
---

<Tag class={`mx-auto max-w-7xl px-6 lg:px-12 ${className}`}>
  <slot />
</Tag>
